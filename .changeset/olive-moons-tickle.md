---
"datocms-plugin-sdk": minor
---

Track `@datocms/cma-client` 6.x, and say `RawApiTypes` instead of the legacy `SchemaTypes` alias

The dependency on `@datocms/cma-client` moves from `^5.0.0` to `^6.1.0`.
Nothing the SDK exports changes.