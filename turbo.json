{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "manifest.json",
        "src/manifest.ts",
        "styles.css",
        "types.json"
      ]
    }
  }
}
